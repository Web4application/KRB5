name: Org CI Baseline

on:
  pull_request:
  push:

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Block plaintext secrets
        uses: gitleaks/gitleaks-action@v2
